server:
  port: 28080

spring:
  application:
    name: java-orm-in-sbt-comparison
  datasource:
    url: jdbc:mysql://localhost:3307/orm_comparison_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true&rewriteBatchedStatements=true
    username: zhaochun1
    password: zhaochun@GITHUB
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: true
      connection-test-query: SELECT 1
      connection-timeout: 300000
      idle-timeout: 180000
      max-lifetime: 1800000
      maximum-pool-size: 10
      minimum-idle: 2
      pool-name: HikariCP-java-orm-in-sbt-comparison-test
  jpa:
    hibernate:
      ddl-auto: none
      jdbc:
        batch_size: 100
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.zhaochuninhefei.orm.comparison: INFO
    org.springframework.test: INFO
    com.zhaochuninhefei.orm.comparison.mybatis: INFO